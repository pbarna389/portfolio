import Layout from "../layout/Main";

const Contracts:React.FC = ():JSX.Element => {
    return (
        <div className="contacts">
            <Layout text="This is the contacts page" />
        </div>
    )
}

export default Contracts;